Import ('s3env')

CppLibProject(
	s3env, 's3-admin', s3env['S3VERSION'], 
	source_files = s3env.Glob ('*.cpp'),
	dependencies = ['s3-security.cpplib', 's3-interfaces.cpplib', 's3-server.cpplib', 's3-db.cpplib', 's3-html.cpplib']
)
