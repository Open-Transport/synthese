Import ('s3env')

senv = s3env.Clone ()

prj = CppLibProject (senv, 's3-messages', s3env['S3VERSION'], 
                     source_files = senv.Glob ('*.cpp'),
                     dependencies = ['s3-security.shlib'])
senv.SConsProject (prj)

