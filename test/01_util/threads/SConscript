Import ('s3env')

senv = s3env.Clone ()
prj = CppTestProject (senv, 's3-util-threads-test', s3env['S3VERSION'], source_files = senv.Glob ('*Test.cpp'),
                     dependencies = ['s3-util-threads.shlib'])
senv.SConsProject (prj)
