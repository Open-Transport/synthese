*** Settings ***
Documentation     Bla bla bla
Resource          sae.txt

*** Variables ***

*** Keywords ***
Le bus ${vehicleId} prend son service ${serviceId} sur la ligne ${commercialLineId}
    [Documentation]    FAKE
    Update Vehicle Position    vehicleId=${vehicleId}    serviceId=${serviceId}

Le bus ${vehicleId} transmet une demande de rappel normale à la régulation
    [Documentation]    FAKE
    Create Vehicle Call    vehicleId=${vehicleId}    priority=false

Le bus ${vehicleId} transmet une demande de rappel urgente à la régulation
    [Documentation]    FAKE
    Create Vehicle Call    vehicleId=${vehicleId}    priority=true
