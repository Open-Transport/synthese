*** Settings ***
Documentation     Bla bla bla
Resource          sae.txt

*** Variables ***

*** Keywords ***

Le bus ${vehicleId} prend son service ${serviceId} sur la ligne ${commercialLineId}
    [Documentation]    FAKE
    Update Vehicle Position    vehicleId=${vehicleId}    serviceId=${serviceId}

Le bus ${vehicleId} transmet une demande de rappel normale à la régulation
    [Documentation]    FAKE
    Le bus ${BUS1} prend son service ${SERVICE1} sur la ligne ${LINE1}
    Create Vehicle Call    vehicleId=${vehicleId}    priority=0

Le bus ${vehicleId} transmet encore une demande de rappel normale à la régulation
    Le bus ${vehicleId} transmet une demande de rappel normale à la régulation


Le bus ${vehicleId} transmet une demande de rappel urgente à la régulation
    [Documentation]    FAKE
    Le bus ${BUS1} prend son service ${SERVICE1} sur la ligne ${LINE1}
    Create Vehicle Call    vehicleId=${vehicleId}    priority=1
